using UnityEngine;

public class PlayerBouncer : MonoBehaviour
{


/*
    void OnTriggerEnter(Collider other)
    {
        // تحقق إذا كان التصادم مع رأس العدو
        if (other.CompareTag("EnemyHead"))
        {
            // تنفيذ القفز القوي (Stomp)
            var player = GetComponentInParent<CharMovement>();
            if (player != null)
            {
                player.AutoJumpOnEnemyHead();
            }

            // تحقق إذا كان الكائن الذي اصطدم به يحتوي على Tag "Enemy"
            if (other.CompareTag("Enemy"))
            {
                // تدمير كائن العدو بالكامل
                Destroy(other.transform.root.gameObject);
            }
        }
    } */
}